package com.sixsense.tangerine.community;

import android.view.View;

public interface OnMarketPostItemClickListener {
    void onItemClick(MarketPostAdapter.ViewHolder holder, View view, int position);
}
