package com.sixsense.tangerine.community;

import android.view.View;

public interface OnCookingTipListItemClickListener {
    void onItemClick(CookingTipListAdapter.ViewHolder holder, View view, int position);
}
